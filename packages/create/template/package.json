{
  "name": "{{name}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch run src/main.ts",
    "start": "bun run src/main.ts",
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "auth:secret": "bun --print 'Array.from(crypto.getRandomValues(new Uint8Array(32))).map(b => b.toString(16).padStart(2, \"0\")).join(\"\")'"
  },
  "dependencies": {
    "@modern-admin/adapter-prisma": "^{{modernAdminVersion}}",
    "@modern-admin/auth-better-auth": "^{{modernAdminVersion}}",
    "@modern-admin/cache-redis": "^{{modernAdminVersion}}",
    "@modern-admin/core": "^{{modernAdminVersion}}",
    "@modern-admin/nest": "^{{modernAdminVersion}}",
    "@modern-admin/system-prisma": "^{{modernAdminVersion}}",
    "@modern-admin/web": "^{{modernAdminVersion}}",
    "@better-auth/api-key": "^1.6.23",
    "@nestjs/common": "^11.1.19",
    "@nestjs/core": "^11.1.19",
    "@nestjs/platform-express": "^11.1.19",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "@prisma/client-runtime-utils": "^7.8.0",
    "@prisma/internals": "^7.8.0",
    "better-auth": "^1.6.23",
    "ioredis": "^5.10.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "prisma": "^7.8.0",
    "typescript": "^6.0.3"
  }
}
